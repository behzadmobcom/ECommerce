@model List<Entities.ViewModel.CategoryParentViewModel>

@if (Model != null)
{
	@*@foreach (var category in Model)
		{
		var path = $"/Shop/{category.Path}";
		<div class="col">
		<div class="megamenu-wrap">
		@if (category.Depth == 0)
		{
		<a href="@path">
		<h5 class="megamenu-title">@category.Name</h5>
		</a>
		}
		@if (category.Children != null)
		{
		<ul class="megamenu-list">
		<partial name="_categoriesMenu" model="category.Children" />
		</ul>
		}
		</div>
		</div>
		}*@

	<div class="col-md-4">
		<div class="nav flex-column nav-pills me-3" id="v-pills-tab" role="tablist" aria-orientation="vertical">
			@foreach (var parentCategory in Model)
			{
				<button class="nav-link" id="v-pills-@parentCategory.Id-tab" data-bs-toggle="pill" data-bs-target="#v-pills-@parentCategory.Id" type="button" role="tab" aria-controls="v-pills-@parentCategory.Id" aria-selected="false">@parentCategory.Name</button>
			}
		</div>
	</div>


	<div class="col-md-8 bg-light">
		<div class="tab-content" id="v-pills-tabContent">
			@foreach (var parent in Model)
			{
				@foreach (var childrenCategory in parent.Children)
				{

					<div class="tab-pane fade" id="v-pills-@parent.Id @childrenCategory.Id" role="tabpanel" aria-labelledby="v-pills-@parent.Id-tab">@childrenCategory.Name</div>
					@*@foreach (var children in childrenCategory.Children)
					{
						<label style="color:red">@children.Name</label><br/>
					}*@
				}
			}
		</div>
	</div>

}


@*<div class="tab-pane fade show active" id="v-pills-home" role="tabpanel" aria-labelledby="v-pills-home-tab">1</div>*@
